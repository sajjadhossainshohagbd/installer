# Nirapod Installer (Don't Use this Package because it's now Beta release)
- Firstly run...
```bash
 composer require nirapodsoft/installer
```
- Secondly, run..
```bash
 php artisan vendor:publish --tag=nirapod-installer-config
```
 
